# Repository created to practice and learn Terraform

It creates an s3 bucket on the AWS and a storage account, and a container on the azure.
Every folder contains some resources, that must be created in order to achieve success

For each folder, to create the resources you need to first go to the folder and then init, plan and apply

Also you need to export/configure the envs based on the .env.example
