To use this resource you need to create an ssh key that will be used on the function file on vm.tf

You will also need to run the aws-net folder beforehand so that you will have some of its outputs, and you also need to run the backend to have the backend states on the cloud